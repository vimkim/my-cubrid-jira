# [UMBRELLA] 11.4.5 Internal Test 결과 — Bug Bounty Findings

## Description

### 배경

11.4.5 internal test (4/22 ~ 4/27, RND-2734) 에 대해 `bug-bounty-11.4.5` worktree (HEAD `3df9de02b`) 에서 RND-2734 에 등재된 수정/기능개선 티켓들을 대상으로 집중 회귀/엣지 케이스 테스트를 수행한 결과 모음이다. 각 finding 은 개별 JIRA subtask 로 분리 등록 예정이며, 본 이슈는 **index + evidence pack** 으로서 기능한다.

### 목적

- 11.4.5 에 들어간 주요 fix 들의 회귀 상태를 단일 보고서로 집계
- 발견된 bug / 누락 / usability issue 를 subtask 분리를 위한 seed 로 제공
- 재현 스크립트와 근거 로그 경로를 일관 정리

### 테스트 환경

- 브랜치: `bug-bounty-11.4.5` (HEAD: `3df9de02b`)
- 빌드: `just build` (debug_clang preset). 빌드 성공.
- 서버: `cubrid server start testdb`
- 클라이언트: `csql -udba testdb`
- 재현 스크립트/로그: `/tmp/bugbounty11455/*.sql`, `/tmp/bugbounty11455/out_*.txt`

---

## Summary

| # | Finding | 원래 티켓 | Severity | Subtask 제안 | 상태 |
|---|---|---|---|---|---|
| F1 | 문자 리터럴 → NUMERIC 비교 경로 auto-cast precision 15→38 미적용 (7자리 이상 "Cannot coerce") | CBRD-26652 | **High** | 별도 이슈로 분리됨: CBRD-26727 | Bug |
| F2 | 단일 뷰 머지 시 `ORDERED` 미제거 / 주질의 LEADING 있어도 부질의 LEADING 이 append | CBRD-26624 / 26419 | **High** | 별도 이슈로 분리됨: CBRD-26728 | Bug |
| F3 | `DROP IF EXISTS <synonym-name>` (타입 키워드 없음) 가 조용히 no-op (시노님 미삭제) | CBRD-26541 | Medium | subtask | Usability regression |
| F4 | `DROP PROCEDURE / FUNCTION IF EXISTS` 구문 syntax error — 다른 object 와 일관성 부족 | (무관, 구조적) | Medium | subtask | UX gap |
| F5 | `/*+ NO_MERGE */` 힌트가 인자 없이 쓰인 경우 inline view 병합 방지 동작 불분명 | CBRD-26624 근처 | Low | subtask 필요 시 | Needs clarification |
| F6 | `Num_query_mjoins` 카운터의 session/process 집계 범위 모호 (3회 merge join 후에도 0 관측) | CBRD-26351 | Low | subtask (검증 필요) | Needs verification |
| V1~V11 | Verified OK (아래 Verified section) | 각 티켓 | — | — | OK |

---

## Findings

### F1. [NUMERIC] 비교 경로 auto-cast precision 미적용 — CBRD-26727

**배경**: CBRD-26652 가 문자 리터럴 → NUMERIC auto-cast default precision 을 `DB_DEFAULT_NUMERIC_PRECISION`(15) → `DB_MAX_NUMERIC_PRECISION`(38) 로 확장. `src/parser/type_checking.c:4815` 한 줄 수정(`pt_coerce_expression_argument`). 그러나 **산술 경로에만 적용** 되고 **비교/BETWEEN/CASE/UPDATE WHERE** 경로에는 미반영.

**재현**:
```sql
CREATE TABLE tnum(v NUMERIC(38,9)); INSERT INTO tnum VALUES (1234567);

SELECT COUNT(*) FROM tnum WHERE v > '9999999';                   -- ERROR 7자리
SELECT COUNT(*) FROM tnum WHERE v BETWEEN '1' AND '9999999';     -- ERROR
SELECT COUNT(*) FROM tnum WHERE v = '1234567';                   -- ERROR
SELECT COUNT(*) FROM tnum WHERE v != '1234567';                  -- ERROR
SELECT COUNT(*) FROM tnum WHERE v <= '1234568';                  -- ERROR
SELECT CASE WHEN v > '1000000' THEN 'b' ELSE 's' END FROM tnum;  -- ERROR
UPDATE tnum SET v = v WHERE v > '1000000';                       -- ERROR

SELECT COUNT(*) FROM tnum WHERE v IN ('1000000','1234567');      -- OK
SELECT v + '9999999'  FROM tnum;                                 -- OK
SELECT v > 9999999   FROM tnum;                                  -- (integer literal) OK
```

**경계값**: 정확히 **정수부 6자리 (`'999999'`)** 까지만 비교 경로가 동작. 이는 기존 `NUMERIC(15, 9)`(정수부 15−9=6) 그대로.

**의심 위치**: `pt_wrap_with_cast_op`, `tp_value_coerce`, 비교 operator 경로의 string→numeric coercion.

**자세한 이슈**: `/home/vimkim/gh/my-cubrid-jira/issues/CBRD-26727-numeric-compare-coerce.md`

---

### F2. [PARSER] 단일 뷰 머지 시 ORDERED/LEADING 힌트 정제 미동작 — CBRD-26728

**배경**: CBRD-26419 의 spec 은 (rule 2) 주질의에 LEADING 이 있으면 부질의 LEADING 은 머지된 질의로 복사하지 않는다, (rule 3) 부질의의 ORDERED 힌트는 뷰 머지 시 제거한다. 11.4.5 빌드에서 `SHOW TRACE` 로 rewritten query 를 확인하면 **단일 뷰 머지** 경로에서 두 규칙이 전혀 동작하지 않음.

**재현**:
```sql
SET TRACE ON;
-- Case A: ORDERED 제거되어야 함
SELECT /*+ recompile */ *
FROM ( SELECT /*+ ORDERED */ a.v FROM t1 a, t2 b WHERE a.id = b.id ) v;
SHOW TRACE;
-- rewritten: select /*+ ORDERED */ v.v ...   ← ORDERED 남음 (제거되어야 함)

-- Case B: outer LEADING 이 있으면 inner LEADING 은 복사되지 않아야 함
SELECT /*+ recompile LEADING(V) */ *
FROM ( SELECT /*+ LEADING(b, a) */ a.v FROM t1 a, t2 b WHERE a.id = b.id ) V;
SHOW TRACE;
-- rewritten: select /*+ LEADING(V, b, a) */ ...   ← inner LEADING append 됨 (복사되면 안 됨)

-- Case C (baseline): outer LEADING 없고 inner LEADING 만 있으면 복사 OK
SELECT /*+ recompile */ *
FROM ( SELECT /*+ LEADING(b, a) */ a.v FROM t1 a, t2 b WHERE a.id = b.id ) V;
-- rewritten: select /*+ LEADING(b, a) */ ...   ← OK (의도대로)
```

**근본 원인**: `src/parser/view_transform.c:14702` `mq_copy_sql_hint()`

```c
/* remove some hints if there are multiple tables.  */
if (dest_query->info.query.q.select.from->next != NULL)  // ← gate
  {
    /* ignore ordered hint */
    if (src_query->info.query.q.select.hint & PT_HINT_ORDERED) ...
    if (src_query->info.query.q.select.hint & PT_HINT_LEADING
        && dest_query->info.query.q.select.hint & PT_HINT_LEADING) ...
  }
```

이 함수는 호출 사이트(`view_transform.c:2597/2798/2838`)에서 **치환 전** 에 실행된다. 단일 뷰 머지 시 `dest->from` 은 view-spec 한 개뿐 → `from->next == NULL` → gate false → 정제 블록 skip → 이어지는 `parser_append_node` 가 무조건 LEADING 을 append.

**자세한 이슈**: `/home/vimkim/gh/my-cubrid-jira/issues/CBRD-26728-viewmerge-hint-gate.md`

추가 관측:
- Case D: outer `/*+ LEADING(V) */` + inner `/*+ ORDERED */` → rewritten 은 `/*+ ORDERED */ V.v from t1 V, t2 b` (LEADING 이 누락되고 ORDERED 만 남음) — 또 다른 정제 실패 패턴
- Case E: outer `/*+ NO_MERGE */` (인자 없음) 에도 불구하고 Query Plan 이 2-테이블 flat scan 으로 rewrite → NO_MERGE 가 무시되는 것처럼 보임. 인자 없는 NO_MERGE 의 의미 확인 필요 (F5 와 중첩 가능).

---

### F3. [DDL] `DROP IF EXISTS <synonym-name>` (타입 키워드 없음) 이 조용히 no-op

**배경**: CBRD-26541 이전에는 `DROP IF EXISTS syn_name` 이 **backing table 을 drop 하는** 버그가 있었다. 11.4.5 에서는 backing table 은 보존된다 (fix OK). 그러나 **시노님 자체도 삭제되지 않고** `Execute OK` 로 조용히 종료된다.

**재현**:
```sql
CREATE TABLE real_t(id INT);
CREATE SYNONYM syn_for_real_t FOR real_t;
DROP IF EXISTS syn_for_real_t;                                  -- Execute OK
SELECT synonym_name FROM db_synonym WHERE synonym_name='syn_for_real_t';
-- → 'syn_for_real_t' (아직 존재)
```

**권고** (택 1):
1. `DROP IF EXISTS <name>` 에 타입 키워드가 없을 때 시노님을 우선 탐색하여 drop
2. 명확히 "cannot determine object type" 오류 발생
3. 현재 동작을 유지하되 문서에 "타입 키워드 필수" 명시

**Impact**: 고객/자동화 스크립트가 DROP 성공으로 오인, 이후 `CREATE SYNONYM` 이 "already exists" 에러로 실패.

---

### F4. [DDL] `DROP PROCEDURE / FUNCTION IF EXISTS` 가 syntax error

**재현**:
```sql
DROP TABLE     IF EXISTS t;       -- OK
DROP VIEW      IF EXISTS v;       -- OK
DROP SYNONYM   IF EXISTS s;       -- OK
DROP PROCEDURE IF EXISTS p;       -- ERROR: Syntax error: unexpected 'IF'
DROP FUNCTION  IF EXISTS f;       -- ERROR: Syntax error: unexpected 'IF'
```

**Impact**: PL/CSQL 관련 스크립트를 idempotent 하게 작성하기 위해 `CREATE OR REPLACE` 는 가능하나, 관리형 드롭이 어려움. MySQL/PostgreSQL/Oracle 모두 `DROP PROCEDURE IF EXISTS` 를 지원.

**권고**: parser 의 `drop_stmt` / `drop_procedure_stmt` rule 에 `IF EXISTS` 옵션 추가. 실패 시 warning 으로만 처리.

---

### F5. [HINT] `/*+ NO_MERGE */` 인자 없이 쓰인 경우 동작 불분명

**관측**: F2 의 Case E 에서 `SELECT /*+ recompile NO_MERGE */ * FROM (SELECT /*+ ORDERED */ ... ) V;` 를 실행했을 때 `SHOW TRACE` 의 rewritten query 가 이미 view-merged 된 형태(`from [dba.t1] V, [dba.t2] b`). CUBRID 매뉴얼의 NO_MERGE 사용법과 인자 없음 케이스의 기대 동작 확인 필요.

**제안**: 구문 검증 + 문서 확인 후 (a) NO_MERGE 무인자 허용 & 전체 뷰 머지 차단, 또는 (b) parser 수준에서 에러로 처리.

---

### F6. [STAT] `Num_query_mjoins` 카운터 집계 범위 확인 필요

**관측**: 서버 restart 후 baseline `Num_query_mjoins = 0` 에서 클라이언트 csql 로 3회 `SELECT /*+ USE_MERGE */ ...` 수행 후에도 `cubrid statdump testdb` 는 `Num_query_mjoins = 0`. 다른 세션/트레이스 경로를 통해 실행한 merge join 에서는 `Num_query_mjoins = 1` 증가 관측.

**가설**:
1. statdump 가 특정 session/process 의 stats 만 표시 (process-local)
2. `MERGELIST (parallel workers: 2)` 경로는 mjoins counter 와 별개
3. counter 증가 타이밍/flush 가 csql 종료 시까지 지연

**다음 probe**: `cubrid statdump -c` (cumulative), 서버 내장 stats 직접 확인, CBRD-26351 원본 repro (RND-2614) 의 기대 카운트와 비교.

---

## Verified OK (no regression found)

| # | 티켓 | 재현 요약 | 결과 |
|---|---|---|---|
| V1 | CBRD-26155 | PL/CSQL static SQL 에 `?` bind param | Compile-time error OK |
| V2 | CBRD-26504 | DELETE/UPDATE 안 START WITH + CONNECT BY, NOCYCLE, JOIN, UNION, WITH CTE | 의도대로 모든 자손 처리 OK |
| V3 | CBRD-26513 | `ALTER PROCEDURE ... COMMENT 'x'` 후 `_db_stored_procedure.comment` 업데이트 | 반영됨 OK |
| V4 | CBRD-26514 | `ALTER PROCEDURE dba.poo OWNER TO public`, 다시 TO dba | 정상 왕복 OK |
| V5 | CBRD-26520 | 중첩 SP `addtwo(addtwo(1,2),4)=7`, 재귀 `recfact(5)=120`, GROUP BY/HAVING/ORDER BY/UNION/CASE 내 SP | 모두 정상 OK |
| V6 | CBRD-26457 | `adddate/subdate` on 2024-02-29, 2000-02-29, 2100-02-28, 1900/2000 century | MySQL-style 기대값 일치 OK |
| V7 | CBRD-25899 | `CONNECT_BY_ROOT` / `SYS_CONNECT_BY_PATH` in GROUP BY, HAVING | 모두 reject OK. SELECT/ORDER BY 에서는 정상 허용 OK |
| V8 | CBRD-26541 | `DROP IF EXISTS syn` 후 backing table 보존 | OK (단 F3 참고) |
| V9 | CBRD-26527 | 대용량 테이블 DROP 후 재생성, 동일 row count 재삽입 | 성공 OK |
| V10 | CBRD-26645 | `cubrid addvoldb --db-volume-size=20M -t temp testdb` | 임시 temp 볼륨 추가 OK, 서버 재시작 시 자동 삭제 OK |
| V11 | CBRD-26703 | `max_hash_list_scan_size=1` + 10K rows hash join | 실패 없이 완료 OK |

---

## Not Yet Tested / Remaining Tickets

아래 티켓은 시간 제약 또는 환경 제약으로 이번 사이클에서 미검증. 후속 subtask 필요.

| 티켓 | 내용 | 제약 |
|---|---|---|
| CBRD-26374 | 11.2~11.3 unloaddb 스키마를 11.4+ 에 `--no-user-specified-name` 으로 load 시 적용 범위 | 과거 버전 unload 파일 필요 |
| CBRD-26428 | Java 의존성 패치 (commons-compress, commons-lang3, netty) | 라이브러리 버전 확인 (static) |
| CBRD-26494 | SP body 출력 시 불필요 `;` 제거 | unloaddb 스키마 비교 필요 |
| CBRD-26497 | unloaddb 시 SP body 끝에 `END <name> COMMENT '...';` 조합의 문법 오류 제거 | CREATE PROCEDURE … COMMENT 구문 확인 필요 |
| CBRD-26545 | `ha_make_slavedb.sh` 명령어 오류 수정 | HA 환경 필요 (master/slave host) |
| CBRD-26578 | `_db_stored_procedure_code.scode` 에 body text 저장 | 테스트 SP 로는 row 미생성 — static SQL 여부 영향 가능 |
| CBRD-26613 | pl_server 통신 에러 7종 식별 개선 | pl_server fault injection 필요 |
| CBRD-26698 | shard_key length > 128 에러 수정 | shard broker 환경 필요 |

---

## Acceptance Criteria (of this umbrella)

- [ ] F1~F6 각각에 대해 개별 JIRA subtask 등록 (제공된 재현 SQL 포함)
- [ ] F1 은 CBRD-26727 로, F2 는 CBRD-26728 로 등록 완료
- [ ] F3 ("DROP IF EXISTS synonym silent no-op") subtask 의 동작 결정 (drop vs error)
- [ ] F4 ("DROP PROCEDURE/FUNCTION IF EXISTS") 지원 여부 결정 (supported/documented)
- [ ] F5/F6 은 추가 조사 후 subtask 승격 여부 판단
- [ ] 미검증 티켓(Not Yet Tested) 의 별도 테스트 세션 일정 협의 (unloaddb/HA/broker 환경)

---

## Remarks

- Session 로그는 `/tmp/bugbounty11455/` 보존됨. 서버 재시작 시 초기화되지 않음.
- 별도 요약 파일: `/home/vimkim/gh/cb/bug-bounty-11.4.5/FINDINGS_11.4.5.md` (간이 영문 버전).
- 이번 세션 빌드: `just build` (debug_clang), HEAD `3df9de02b`.
- 이번 테스트는 단일 노드 standalone 모드. HA/broker/sharding 시나리오는 커버되지 않음.
- 별도 등록된 이슈:
  - CBRD-26727: [NUMERIC] 비교 연산자 auto-cast precision 미적용
  - CBRD-26728: [PARSER] 단일 뷰 머지 시 ORDERED/LEADING 힌트 정제 미동작

---

## Deep Analysis Addendum (2nd pass)

2nd pass 에서 추가 발견한 사실과 **근본 원인 좌표**, 그리고 추가 negative case (verified OK / recent-commit 회귀 없음) 를 정리.

### A1. F1 — 근본 원인 좌표 특정

F1 (CBRD-26727) 의 실제 defect site 는 **CBRD-26652 에서 고친 `pt_coerce_expression_argument` 가 아니라 `pt_upd_domain_info()`** 이다.

- 파일: `src/parser/type_checking.c`
- 함수: `pt_upd_domain_info(parser, arg1, arg2, op, common_type, ...)` at line 11245
- 주석: _"preparing a node for a binary operation"_
- Hardcoded bug site:
  ```c
  // line 11292~11297
  else if (arg1 && arg1->type_enum == PT_TYPE_NUMERIC)
    {
      arg1_prec     = DB_DEFAULT_NUMERIC_PRECISION;   // ← 15 하드코딩
      arg1_dec_prec = DB_DEFAULT_NUMERIC_SCALE;
      arg1_units    = 0;
    }
  // line 11339~11344  (symmetrical for arg2)
  ```

이 함수는 **comparison operator (PT_GT, PT_EQ, PT_LE …) / BETWEEN / CASE** 등의 argument precision 을 결정할 때 호출된다. `arg->data_type == NULL` 이면 `DB_DEFAULT_NUMERIC_PRECISION`(15) 로 폴백하므로, 컬럼의 실제 precision 이 NUMERIC(38,0) 이나 NUMERIC(20,5) 이어도 comparison 에서는 (15,9) 취급된다.

반면 CBRD-26652 에서 수정한 `pt_coerce_expression_argument()` 는 주로 **arithmetic operator (PT_PLUS / PT_MINUS / PT_TIMES / PT_DIVIDE)** 경로에서 호출되므로 산술은 정상 동작.

### A2. F1 — 경로별 동작 확정 (evidence)

같은 NUMERIC(38,9) 컬럼에 대해 7자리 이상 문자 리터럴을 주는 경로별:

| 경로 | 예시 | 동작 | 비고 |
|---|---|---|---|
| 비교 (col op str) | `v > '9999999'` | ❌ ERROR | 7자리 경계 |
| 비교 (str op col) | `'9999999' > v` | ❌ ERROR | 대칭적 실패 |
| BETWEEN | `v BETWEEN '1' AND '9999999'` | ❌ ERROR | — |
| CASE | `CASE WHEN v > '1000000' THEN ...` | ❌ ERROR | — |
| UPDATE WHERE | `UPDATE ... WHERE v > '1000000'` | ❌ ERROR | — |
| =, !=, <= | `v = '1234567'`, `v != ...`, `v <= ...` | ❌ ERROR | — |
| IN 리스트 | `v IN ('1000000','1234567')` | ✅ OK | IN은 별도 경로 |
| 산술 | `v + '9999999'`, `v * '2'` | ✅ OK | CBRD-26652 적용됨 |
| INSERT VALUES | `INSERT INTO t VALUES ('9999999')` | ✅ OK | 런타임 cast |
| Session var | `SET @x='9999999'; v > @x` | ✅ OK | 런타임 cast |
| Non-NUMERIC 컬럼 | `DATE/TIME/DATETIME/CHAR/ENUM/JSON/DOUBLE` | ✅ OK | NUMERIC 전용 |

**결론**: parse-time **문자 리터럴 → NUMERIC 컬럼** 비교 경로만 (15,9) 로 강제된다. 런타임 경로는 정상. → CBRD-26727 의 Acceptance Criteria 에 "literal 과 host-var 가 동일하게 동작" 를 추가할 수 있음.

### A3. F4 — `IF EXISTS` 지원 매트릭스

| DDL | `IF EXISTS` | 상태 |
|---|---|---|
| `DROP TABLE`     | ✅ | OK |
| `DROP VIEW`      | ✅ | OK |
| `DROP SYNONYM`   | ✅ | OK |
| `DROP SERIAL`    | ✅ | OK |
| `DROP TRIGGER`   | ❌ | Syntax error |
| `DROP INDEX`     | ❌ | Syntax error |
| `DROP USER`      | ❌ | Syntax error |
| `DROP PROCEDURE` | ❌ | Syntax error |
| `DROP FUNCTION`  | ❌ | Syntax error |

스크립트의 idempotency 확보를 위해 적어도 `TRIGGER / INDEX / PROCEDURE / FUNCTION` 은 추가 지원 권장. MySQL/PostgreSQL/Oracle 모두 지원.

### A4. F2 — 추가 증거 (CTE / 중첩 inline view)

CBRD-26728 의 `mq_copy_sql_hint()` gate bug 는 **CTE 와 중첩 inline view** 에서도 동일하게 관측.

```sql
-- CTE 형태
WITH V AS ( SELECT /*+ ORDERED */ a.v FROM t1 a, t2 b WHERE a.id = b.id )
SELECT /*+ recompile */ * FROM V;
-- rewritten: select /*+ ORDERED */ V.v from [dba.t1] V, [dba.t2] b where (V.id=b.id)

-- 중첩 inline view (2-deep)
SELECT /*+ recompile */ * FROM
  ( SELECT * FROM ( SELECT /*+ ORDERED */ a.v FROM t1 a, t2 b WHERE a.id = b.id ) iv ) v;
-- rewritten: select /*+ ORDERED */ v.v from [dba.t1] v, [dba.t2] b where (v.id=b.id)
```

두 경우 모두 ORDERED 힌트가 병합된 query 로 전파된다. CBRD-26728 의 fix 가 CTE/다중 레벨 머지에도 적용되어야 한다는 점을 명시 필요.

DML (UPDATE … WHERE IN (SELECT /*+ ORDERED */ … FROM t1, t2)) 은 **subquery 가 derived 로 남아** view-merge 대상이 아님 → ORDERED 가 subquery 범위 내에서만 유효 → 의도대로 동작. 이는 별도 bug 아님.

### A5. 추가 Verified OK (최신 커밋)

이번 2nd pass 에서 최근 3df9de02b HEAD 에 포함된 티켓들을 추가 검증:

| 티켓 | 검증 | 결과 |
|---|---|---|
| CBRD-26495 | `ORDER BY CONNECT_BY_ROOT(c1)` with `GROUP BY c1` | Semantic error 로 reject ✓ (fix 의도대로) |
| CBRD-26519 | 한 테이블에 2개 이상 AUTO_INCREMENT (CREATE/ALTER) | 모두 reject ✓ |
| CBRD-26555 | SP 내 반복 `SELECT ... INTO` 에 row 없음, 5000회 반복 | "Cannot allocate query entry" 미발생 ✓ |
| CBRD-26571 | `(SELECT COUNT(*) FROM pk_a) + (SELECT COUNT(*) FROM pk_b)` 등 UNION + PK count | 결과 정확 ✓ |
| CBRD-26647 | `SELECT DBA.myser.NEXTVAL` | 정상 ✓ (dblink 환경 아님) |
| CBRD-26670 | session var (`@v`) in analytic + parallel subquery | Crash 없이 정상 결과 ✓ |
| CBRD-26707 | parallel heap scan + NOT EXISTS + filter subquery | Crash 없이 실행 ✓ |
| CBRD-26712 | 조인 selectivity 최소값 개선 | 결과 정확 ✓ (plan 검토 필요) |

### A6. 추가 subtask 후보 (A1~A4 기반)

- **CBRD-26727 보강**: A1 의 코드 좌표 (`pt_upd_domain_info`), A2 의 경로 매트릭스 추가 후 재검토.
- **F4 (DROP IF EXISTS 확장)**: 현재 지원 4종 → 9종으로 확장 제안. 단일 subtask 로 묶기 vs 객체별 분리는 팀 선호.
- **F2 보강** (CBRD-26728): A4 의 CTE/중첩 케이스 추가. Acceptance Criteria 에 CTE 증거 포함.
- **NOT_ISSUE 전환**: F6 (`Num_query_mjoins`) 은 2nd pass 에서 session 바꿔 재현하면 정상 증가 확인 가능. Not-a-bug 로 close 추천.
